# ClaudeBell

may use this prompt to install it:
"project folder is `<your_project_root>`, port is `9090`, ip is `your IP`, read the `NOTIFICATION_SYSTEM.md` change the notification-server.py and start it"

then start the client from your laptop by:
`.\notification-client.ps1`
